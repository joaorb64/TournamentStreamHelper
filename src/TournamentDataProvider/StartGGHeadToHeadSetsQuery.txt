query HeadToHeadSetsQuery($eventId: ID!, $pid: ID!, $page: Int!) {
	event(id: $eventId) {
        sets(page: $page, perPage: 500, filters: {playerIds: [$pid]}) {
            nodes {
                entrant1Score
                entrant2Score
                winnerId
                slots(includeByes: false) {
                    entrant {
                        id
                        participants {
                            id
                            player {
                                id
                            }
                        }
                    }
                }
            }
        }
    }
}